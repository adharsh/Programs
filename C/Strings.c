#include <stdio.h>
#include <string.h>

int main(){

	char a[100], b[100];

	printf("Enter a String: ");
	gets(a);
	
	printf("\nEnter in String2: ");
	        /* get */s(b);

	printf("String a: %s\t String b: %s\t", a, b);

}